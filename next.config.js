/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/honeymoon-2026',
  assetPrefix: '/honeymoon-2026',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
